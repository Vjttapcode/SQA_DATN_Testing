# Test Cases cho Chức Năng Hậu Kiểm Thời Khóa Biểu

Ghi chú:
- Phân loại theo đúng thứ tự: `UI testing` -> `validation test` -> `function test`.
- Cột `Kỹ thuật kiểm thử` chỉ dùng 5 kỹ thuật: `Equivalence Class Partitioning`, `Boundary Value Analysis`, `Decision Table`, `State Transition`, `Pairwise testing`.
- Hai cột `Kết quả thực tế` và `Trạng thái` để trống.

## UI testing

| Mã trường hợp kiểm thử | Chức năng | Mục đích kiểm thử | Kỹ thuật kiểm thử | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| HVK-001 | Header trang | Kiểm tra trang hiển thị đúng tiêu đề chức năng hậu kiểm | Equivalence Class Partitioning | Người dùng đã đăng nhập | 1. Mở `/schedule-validation`.<br>2. Quan sát header. | Hiển thị tiêu đề `Hậu kiểm Thời khóa biểu` và mô tả upload file kiểm tra xung đột. |  |  |
| HVK-002 | Khu vực upload | Kiểm tra card upload hiển thị đúng tiêu đề | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát card bên trái. | Hiển thị tiêu đề `Upload file kiểm tra`. |  |  |
| HVK-003 | Khu vực thông tin | Kiểm tra card thông tin hiển thị đúng tiêu đề | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát card bên phải. | Hiển thị tiêu đề `Thông tin về tính năng`. |  |  |
| HVK-004 | Hướng dẫn sử dụng | Kiểm tra phần hướng dẫn có đủ nội dung chính | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát mục hướng dẫn. | Hiển thị hướng dẫn upload file `.xlsx` và mô tả phát hiện xung đột phòng, giảng viên. |  |  |
| HVK-005 | Vùng kéo thả file | Kiểm tra vùng upload hiển thị đúng nội dung mặc định | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát vùng kéo thả. | Hiển thị thông điệp chọn hoặc kéo thả file `.xlsx`, tối đa 10MB. |  |  |
| HVK-006 | Nút chọn file | Kiểm tra nút `Chọn file` hiển thị trên vùng upload | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát vùng upload. | Nút `Chọn file` hiển thị và sẵn sàng thao tác. |  |  |
| HVK-007 | Nút tải file mẫu | Kiểm tra nút `Tải file mẫu` hiển thị | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát vùng thao tác dưới card upload. | Nút `Tải file mẫu` hiển thị. |  |  |
| HVK-008 | Nút phân tích | Kiểm tra nút `Kiểm tra xung đột` hiển thị đúng | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát vùng thao tác dưới card upload. | Nút `Kiểm tra xung đột` hiển thị. |  |  |
| HVK-009 | Trạng thái nút phân tích | Kiểm tra nút phân tích bị khóa khi chưa chọn file | Boundary Value Analysis | `selectedFile = null` | 1. Mở trang.<br>2. Quan sát nút `Kiểm tra xung đột`. | Nút phân tích bị disable khi chưa có file. |  |  |
| HVK-010 | Card file đã chọn | Kiểm tra card thông tin file chỉ hiển thị sau khi chọn file | State Transition | Chưa chọn file, sau đó chọn 1 file hợp lệ | 1. Mở trang.<br>2. Chọn 1 file hợp lệ.<br>3. Quan sát card file. | Trước khi chọn file card không hiển thị; sau khi chọn file card hiển thị tên và kích thước file. |  |  |
| HVK-011 | Drag and drop | Kiểm tra vùng upload đổi style khi kéo file vào | State Transition | Kéo file vào vùng drop | 1. Kéo file vào vùng upload.<br>2. Giữ file phía trên vùng drop.<br>3. Quan sát style. | Vùng upload đổi style active theo trạng thái drag-over. |  |  |
| HVK-012 | Card thông tin tính năng | Kiểm tra khối `Phát hiện xung đột phòng` hiển thị đúng | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát card thông tin. | Hiển thị khối thông tin về phát hiện xung đột phòng. |  |  |
| HVK-013 | Card thông tin tính năng | Kiểm tra khối `Phát hiện xung đột giảng viên` hiển thị đúng | Equivalence Class Partitioning | Mở trang hậu kiểm | 1. Mở trang.<br>2. Quan sát card thông tin. | Hiển thị khối thông tin về phát hiện xung đột giảng viên. |  |  |
| HVK-014 | Alert lỗi | Kiểm tra banner lỗi hiển thị đúng style khi có lỗi | State Transition | Gây ra lỗi phân tích hoặc lỗi chọn file | 1. Thực hiện thao tác gây lỗi.<br>2. Quan sát vùng alert. | Hiển thị banner màu đỏ với icon cảnh báo và nội dung lỗi. |  |  |
| HVK-015 | Alert thành công | Kiểm tra banner thành công hiển thị đúng style trước khi chuyển sang view kết quả | State Transition | Phân tích file thành công | 1. Chọn file hợp lệ.<br>2. Phân tích file.<br>3. Quan sát trạng thái thành công. | Hệ thống ghi nhận trạng thái thành công; sau đó chuyển sang view kết quả. |  |  |
| HVK-016 | View kết quả | Kiểm tra header kết quả hiển thị đúng khi có `result` | State Transition | `result` có dữ liệu phân tích | 1. Phân tích file thành công.<br>2. Quan sát đầu trang kết quả. | Hiển thị header `Kết quả kiểm tra thời khóa biểu`. |  |  |
| HVK-017 | View kết quả | Kiểm tra nút `Kiểm tra file khác` hiển thị trong màn kết quả | Equivalence Class Partitioning | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Quan sát góc phải header kết quả. | Hiển thị nút `Kiểm tra file khác`. |  |  |
| HVK-018 | Tổng quan kết quả | Kiểm tra summary card `Tổng số xung đột` hiển thị | Equivalence Class Partitioning | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Quan sát summary cards. | Hiển thị card `Tổng số xung đột`. |  |  |
| HVK-019 | Tổng quan kết quả | Kiểm tra summary card `Xung đột phòng` hiển thị | Equivalence Class Partitioning | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Quan sát summary cards. | Hiển thị card `Xung đột phòng`. |  |  |
| HVK-020 | Tổng quan kết quả | Kiểm tra summary card `Xung đột giảng viên` hiển thị | Equivalence Class Partitioning | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Quan sát summary cards. | Hiển thị card `Xung đột giảng viên`. |  |  |
| HVK-021 | Badge trạng thái | Kiểm tra badge `Không có xung đột` hiển thị đúng khi tổng xung đột bằng 0 | Boundary Value Analysis | `totalConflicts = 0` | 1. Phân tích file không có xung đột.<br>2. Quan sát badge trạng thái. | Hiển thị badge xanh `Không có xung đột`. |  |  |
| HVK-022 | Badge trạng thái | Kiểm tra badge `Có xung đột` hiển thị đúng khi tổng xung đột lớn hơn 0 | Boundary Value Analysis | `totalConflicts > 0` | 1. Phân tích file có xung đột.<br>2. Quan sát badge trạng thái. | Hiển thị badge đỏ `Có xung đột`. |  |  |
| HVK-023 | No-conflict panel | Kiểm tra panel chúc mừng hiển thị khi không phát hiện xung đột | State Transition | Kết quả phân tích không có xung đột | 1. Phân tích file không có xung đột.<br>2. Quan sát nội dung chính. | Hiển thị panel xanh với thông điệp không có xung đột. |  |  |
| HVK-024 | Room conflict panel | Kiểm tra panel xung đột phòng hiển thị khi có xung đột phòng | State Transition | `roomConflictCount > 0` | 1. Phân tích file có xung đột phòng.<br>2. Quan sát nội dung kết quả. | Hiển thị panel `Xung đột phòng học` cùng số lượng xung đột. |  |  |
| HVK-025 | Teacher conflict panel | Kiểm tra panel xung đột giảng viên hiển thị khi có xung đột giảng viên | State Transition | `teacherConflictCount > 0` | 1. Phân tích file có xung đột giảng viên.<br>2. Quan sát nội dung kết quả. | Hiển thị panel `Xung đột giảng viên` cùng số lượng xung đột. |  |  |
| HVK-026 | Chi tiết xung đột phòng | Kiểm tra mỗi xung đột phòng hiển thị thành card riêng | Equivalence Class Partitioning | Có ít nhất 1 room conflict | 1. Phân tích file có xung đột phòng.<br>2. Quan sát danh sách card. | Mỗi nhóm xung đột phòng hiển thị thành 1 card riêng với mô tả và danh sách lịch liên quan. |  |  |
| HVK-027 | Chi tiết xung đột giảng viên | Kiểm tra mỗi xung đột giảng viên hiển thị thành card riêng | Equivalence Class Partitioning | Có ít nhất 1 teacher conflict | 1. Phân tích file có xung đột giảng viên.<br>2. Quan sát danh sách card. | Mỗi nhóm xung đột giảng viên hiển thị thành 1 card riêng với mô tả và danh sách lịch liên quan. |  |  |
| HVK-028 | Scroll panel kết quả | Kiểm tra panel xung đột có thể cuộn khi danh sách dài | Equivalence Class Partitioning | Có nhiều conflict items | 1. Phân tích file có nhiều xung đột.<br>2. Cuộn trong panel xung đột. | Panel xung đột cuộn được trong vùng chiều cao giới hạn. |  |  |
| HVK-029 | Chuyển trạng thái màn hình | Kiểm tra khi có `result`, màn hình upload không còn hiển thị | State Transition | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Quan sát toàn bộ trang. | Chỉ hiển thị màn hình kết quả; card upload ban đầu không còn xuất hiện. |  |  |
| HVK-030 | Nút phụ không dùng | Kiểm tra UI hiện tại không hiển thị nút in hoặc xuất báo cáo kết quả | Equivalence Class Partitioning | Đã có hoặc chưa có kết quả | 1. Mở trang hậu kiểm.<br>2. Quan sát UI ở cả màn upload và màn kết quả. | Không có nút in hoặc xuất báo cáo hiển thị trên UI hiện tại. |  |  |

## validation test

| Mã trường hợp kiểm thử | Chức năng | Mục đích kiểm thử | Kỹ thuật kiểm thử | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| HVK-031 | Chọn file | Kiểm tra FE từ chối file `.xls` | Equivalence Class Partitioning | File `sample.xls` | 1. Nhấn `Chọn file`.<br>2. Chọn file `.xls`.<br>3. Quan sát phản hồi. | Hiển thị lỗi yêu cầu chọn file Excel `.xlsx`, file không được nhận. |  |  |
| HVK-032 | Chọn file | Kiểm tra FE từ chối file `.csv` | Equivalence Class Partitioning | File `sample.csv` | 1. Chọn file `.csv`.<br>2. Quan sát phản hồi. | Hệ thống từ chối file và hiển thị lỗi định dạng. |  |  |
| HVK-033 | Chọn file | Kiểm tra FE chấp nhận file `.XLSX` viết hoa đuôi | Equivalence Class Partitioning | File `SCHEDULE.XLSX` hợp lệ | 1. Chọn file `.XLSX`.<br>2. Quan sát card file. | File được chấp nhận vì hệ thống kiểm tra không phân biệt hoa thường. |  |  |
| HVK-034 | Kích thước file | Kiểm tra FE từ chối file lớn hơn 10MB | Boundary Value Analysis | File `.xlsx` có kích thước 10MB + 1 byte | 1. Chọn file quá 10MB.<br>2. Quan sát phản hồi. | Hiển thị lỗi file quá lớn, file không được chọn. |  |  |
| HVK-035 | Kích thước file | Kiểm tra FE chấp nhận file đúng bằng 10MB | Boundary Value Analysis | File `.xlsx` có kích thước đúng 10MB | 1. Chọn file 10MB.<br>2. Quan sát card file. | File được chấp nhận và hiển thị trong card file đã chọn. |  |  |
| HVK-036 | Kích thước file | Kiểm tra FE chấp nhận file 0 byte nếu đúng đuôi `.xlsx` | Boundary Value Analysis | File `.xlsx` 0 byte | 1. Chọn file 0 byte.<br>2. Quan sát trạng thái chọn file. | FE vẫn chấp nhận file; lỗi xử lý sẽ được trả về ở bước phân tích nếu có. |  |  |
| HVK-037 | Nút phân tích | Kiểm tra nhấn phân tích khi chưa có file sẽ tạo lỗi bắt buộc | Decision Table | `selectedFile = null` | 1. Mở trang.<br>2. Gọi thao tác phân tích tương ứng.<br>3. Quan sát lỗi. | Hiển thị lỗi `Vui lòng chọn file Excel`. |  |  |
| HVK-038 | Nút phân tích | Kiểm tra nút phân tích bị disable khi chưa chọn file | Decision Table | Chưa chọn file | 1. Mở trang.<br>2. Quan sát nút phân tích. | Nút bị disable, không thể nhấn. |  |  |
| HVK-039 | Nút phân tích | Kiểm tra nút phân tích bị disable trong lúc đang phân tích | State Transition | Đã chọn file hợp lệ, API phản hồi chậm | 1. Chọn file.<br>2. Nhấn phân tích.<br>3. Giữ API chậm.<br>4. Quan sát nút. | Nút bị disable và đổi sang trạng thái `Đang kiểm tra...`. |  |  |
| HVK-040 | Xóa trạng thái cũ | Kiểm tra chọn file hợp lệ mới sẽ xóa thông báo lỗi cũ | State Transition | Đang có `error`, sau đó chọn file hợp lệ | 1. Gây lỗi trước đó.<br>2. Chọn file hợp lệ.<br>3. Quan sát banner lỗi. | Banner lỗi cũ bị xóa sau khi chọn file hợp lệ. |  |  |
| HVK-041 | Xóa trạng thái cũ | Kiểm tra chọn file hợp lệ mới sẽ xóa thông báo thành công cũ | State Transition | Đang có `success`, sau đó chọn file hợp lệ mới | 1. Phân tích thành công.<br>2. Chọn file mới.<br>3. Quan sát trạng thái. | Trạng thái thành công cũ bị xóa khi chọn file mới. |  |  |
| HVK-042 | Xóa trạng thái cũ | Kiểm tra chọn file mới sẽ xóa kết quả phân tích cũ | State Transition | Đang có `result`, sau đó chọn file mới | 1. Phân tích file đầu tiên.<br>2. Quay về chọn file mới.<br>3. Chọn file khác. | Kết quả cũ bị xóa, hệ thống sẵn sàng phân tích file mới. |  |  |
| HVK-043 | Drag and drop | Kiểm tra kéo thả file sai đuôi cũng bị từ chối như file picker | Equivalence Class Partitioning | Kéo file `.csv` hoặc `.xls` vào vùng drop | 1. Kéo file sai đuôi vào vùng upload.<br>2. Thả file.<br>3. Quan sát phản hồi. | Hiển thị lỗi định dạng, file không được chọn. |  |  |
| HVK-044 | Drag and drop | Kiểm tra kéo thả file quá 10MB cũng bị từ chối | Boundary Value Analysis | Kéo file `.xlsx` > 10MB vào vùng drop | 1. Kéo file quá lớn vào vùng upload.<br>2. Thả file.<br>3. Quan sát phản hồi. | Hiển thị lỗi kích thước, file không được chọn. |  |  |
| HVK-045 | Drag state | Kiểm tra rời chuột khỏi vùng upload sẽ bỏ trạng thái drag-over | State Transition | Đang kéo file hợp lệ vào vùng upload rồi rời ra ngoài | 1. Kéo file vào vùng upload.<br>2. Rời chuột ra ngoài.<br>3. Quan sát style vùng upload. | Vùng upload trở về style mặc định, không còn trạng thái drag-over. |  |  |
| HVK-046 | Reset kết quả | Kiểm tra nút `Kiểm tra file khác` xóa file đã chọn | State Transition | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Nhấn `Kiểm tra file khác`.<br>3. Quan sát card file. | File đã chọn bị xóa, không còn card file cũ. |  |  |
| HVK-047 | Reset kết quả | Kiểm tra nút `Kiểm tra file khác` xóa lỗi hiện tại | State Transition | Đã có lỗi trước đó | 1. Gây lỗi.<br>2. Nhấn reset tương ứng nếu ở màn kết quả hoặc thao tác reset.<br>3. Quan sát banner lỗi. | Lỗi cũ bị xóa. |  |  |
| HVK-048 | Reset kết quả | Kiểm tra nút `Kiểm tra file khác` xóa trạng thái thành công | State Transition | Đã có kết quả thành công | 1. Phân tích thành công.<br>2. Nhấn `Kiểm tra file khác`.<br>3. Quan sát trạng thái. | Trạng thái thành công cũ bị xóa. |  |  |
| HVK-049 | Reset kết quả | Kiểm tra nút `Kiểm tra file khác` xóa `result` | State Transition | Đã có kết quả phân tích | 1. Phân tích file thành công.<br>2. Nhấn `Kiểm tra file khác`.<br>3. Quan sát màn hình. | Kết quả bị xóa và màn upload ban đầu xuất hiện lại. |  |  |
| HVK-050 | Reset input | Kiểm tra reset sẽ xóa giá trị file trong `input type=file` | State Transition | Đã chọn file và có kết quả | 1. Phân tích file.<br>2. Nhấn `Kiểm tra file khác`.<br>3. Mở file picker lại. | Input file đã được reset, không giữ file cũ. |  |  |
| HVK-051 | Lỗi backend | Kiểm tra backend trả `File không đúng định dạng` được hiển thị đúng | Equivalence Class Partitioning | FE gửi file `.xlsx` nhưng backend validate format fail | 1. Chọn file `.xlsx` sai cấu trúc.<br>2. Nhấn phân tích.<br>3. Quan sát phản hồi. | Hiển thị lỗi file không đúng định dạng thời khóa biểu. |  |  |
| HVK-052 | Lỗi backend | Kiểm tra backend trả lỗi `Vui lòng chọn file Excel để upload` nếu file rỗng | Equivalence Class Partitioning | Request gửi file rỗng hoặc bị lỗi upload | 1. Mô phỏng request file rỗng.<br>2. Nhấn phân tích.<br>3. Quan sát phản hồi. | Hệ thống hiển thị thông báo lỗi file rỗng. |  |  |
| HVK-053 | Lỗi backend | Kiểm tra backend trả lỗi `Không tìm thấy dữ liệu thời khóa biểu` khi file không có dòng hợp lệ | Equivalence Class Partitioning | File hợp lệ định dạng nhưng không có entry hợp lệ | 1. Chọn file không có dữ liệu hợp lệ.<br>2. Nhấn phân tích.<br>3. Quan sát phản hồi. | Hiển thị thông báo không tìm thấy dữ liệu thời khóa biểu trong file. |  |  |
| HVK-054 | Lỗi mạng | Kiểm tra lỗi mạng khi phân tích hiển thị thông báo chung | Equivalence Class Partitioning | Mất mạng hoặc API analyze timeout | 1. Chọn file hợp lệ.<br>2. Chặn mạng.<br>3. Nhấn phân tích. | Hiển thị lỗi `Lỗi kết nối. Vui lòng thử lại sau.` |  |  |
| HVK-055 | Tải file mẫu | Kiểm tra lỗi tải file mẫu hiển thị thông báo lỗi nếu download thất bại | Equivalence Class Partitioning | Mô phỏng lỗi khi tải file mẫu | 1. Chặn đường dẫn file mẫu.<br>2. Nhấn `Tải file mẫu`. | Hiển thị lỗi không thể tải file mẫu. |  |  |
| HVK-056 | Tải file mẫu | Kiểm tra nhấn `Tải file mẫu` thành công không tạo alert lỗi | Equivalence Class Partitioning | Đường dẫn file mẫu hợp lệ | 1. Nhấn `Tải file mẫu`.<br>2. Quan sát phản hồi. | File mẫu được tải xuống, không hiển thị banner lỗi. |  |  |
| HVK-057 | Thay file đã chọn | Kiểm tra chọn file hợp lệ mới sẽ thay thế file cũ đã chọn | State Transition | Đã chọn file A, sau đó chọn file B | 1. Chọn file A.<br>2. Chọn tiếp file B.<br>3. Quan sát card file. | Card file hiển thị thông tin file B, không còn file A. |  |  |
| HVK-058 | Hiển thị card file | Kiểm tra card file đã chọn không hiển thị khi chưa có file | Boundary Value Analysis | `selectedFile = null` | 1. Mở trang lần đầu.<br>2. Quan sát dưới vùng upload. | Không hiển thị card file đã chọn. |  |  |
| HVK-059 | Chặn thao tác lặp | Kiểm tra không thể nhấn phân tích lặp lại khi đang xử lý | Decision Table | Đã có file hợp lệ, `isAnalyzing = true` | 1. Chọn file.<br>2. Nhấn phân tích.<br>3. Thử nhấn tiếp trong lúc loading. | Nút bị disable, không phát sinh thao tác phân tích lặp. |  |  |
| HVK-060 | Thuộc tính input file | Kiểm tra input file chỉ accept `.xlsx` ở phía UI | Decision Table | Mở DOM của input file | 1. Mở trang.<br>2. Kiểm tra thuộc tính `accept` của input file. | Thuộc tính `accept` là `.xlsx`. |  |  |

## function test

| Mã trường hợp kiểm thử | Chức năng | Mục đích kiểm thử | Kỹ thuật kiểm thử | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| HVK-061 | Phân tích thành công | Kiểm tra file hợp lệ không có xung đột chuyển sang màn kết quả | Equivalence Class Partitioning | File `.xlsx` hợp lệ, không có conflict | 1. Chọn file hợp lệ không conflict.<br>2. Nhấn phân tích.<br>3. Quan sát màn hình. | Hệ thống chuyển sang màn kết quả và hiển thị tổng xung đột bằng 0. |  |  |
| HVK-062 | Kết quả không xung đột | Kiểm tra summary `Tổng số xung đột` bằng 0 với file không conflict | Boundary Value Analysis | File không có xung đột | 1. Phân tích file không xung đột.<br>2. Quan sát summary card. | Giá trị `Tổng số xung đột` hiển thị 0. |  |  |
| HVK-063 | Kết quả không xung đột | Kiểm tra panel chúc mừng hiển thị khi không có conflict | Equivalence Class Partitioning | File không có xung đột | 1. Phân tích file không xung đột.<br>2. Quan sát phần nội dung kết quả. | Hiển thị panel xanh với thông điệp không phát hiện xung đột. |  |  |
| HVK-064 | Kết quả không xung đột | Kiểm tra panel xung đột phòng không hiển thị khi `roomConflictCount = 0` | Boundary Value Analysis | `roomConflictCount = 0` | 1. Phân tích file không có xung đột phòng.<br>2. Quan sát nội dung kết quả. | Panel xung đột phòng không hiển thị. |  |  |
| HVK-065 | Kết quả không xung đột | Kiểm tra panel xung đột giảng viên không hiển thị khi `teacherConflictCount = 0` | Boundary Value Analysis | `teacherConflictCount = 0` | 1. Phân tích file không có xung đột giảng viên.<br>2. Quan sát nội dung kết quả. | Panel xung đột giảng viên không hiển thị. |  |  |
| HVK-066 | Tổ hợp xung đột | Kiểm tra file có xung đột phòng nhưng không có xung đột giảng viên | Pairwise testing | File chỉ có room conflict | 1. Chọn file chỉ có xung đột phòng.<br>2. Phân tích file.<br>3. Quan sát kết quả. | `roomConflictCount > 0`, `teacherConflictCount = 0`, chỉ hiển thị panel xung đột phòng. |  |  |
| HVK-067 | Tổ hợp xung đột | Kiểm tra card summary hiển thị đúng số lượng xung đột phòng | Equivalence Class Partitioning | File có room conflict | 1. Phân tích file có xung đột phòng.<br>2. Quan sát card `Xung đột phòng`. | Card hiển thị đúng số lượng xung đột phòng theo response. |  |  |
| HVK-068 | Tổ hợp xung đột | Kiểm tra panel room conflict hiển thị số lượng conflict trong tiêu đề | Equivalence Class Partitioning | File có room conflict | 1. Phân tích file có xung đột phòng.<br>2. Quan sát tiêu đề panel. | Tiêu đề panel hiển thị `Xung đột phòng học (n)` với `n` đúng bằng `roomConflictCount`. |  |  |
| HVK-069 | Tổ hợp xung đột | Kiểm tra panel teacher conflict bị ẩn khi không có xung đột giảng viên | Pairwise testing | File chỉ có room conflict | 1. Phân tích file chỉ có room conflict.<br>2. Quan sát panel teacher conflict. | Panel xung đột giảng viên không hiển thị. |  |  |
| HVK-070 | Tổ hợp xung đột | Kiểm tra file có xung đột giảng viên nhưng không có xung đột phòng | Pairwise testing | File chỉ có teacher conflict | 1. Chọn file chỉ có xung đột giảng viên.<br>2. Phân tích file.<br>3. Quan sát kết quả. | `teacherConflictCount > 0`, `roomConflictCount = 0`, chỉ hiển thị panel xung đột giảng viên. |  |  |
| HVK-071 | Tổ hợp xung đột | Kiểm tra card summary hiển thị đúng số lượng xung đột giảng viên | Equivalence Class Partitioning | File có teacher conflict | 1. Phân tích file có xung đột giảng viên.<br>2. Quan sát card `Xung đột giảng viên`. | Card hiển thị đúng số lượng xung đột giảng viên theo response. |  |  |
| HVK-072 | Tổ hợp xung đột | Kiểm tra panel teacher conflict hiển thị số lượng conflict trong tiêu đề | Equivalence Class Partitioning | File có teacher conflict | 1. Phân tích file có xung đột giảng viên.<br>2. Quan sát tiêu đề panel. | Tiêu đề panel hiển thị `Xung đột giảng viên (n)` với `n` đúng bằng `teacherConflictCount`. |  |  |
| HVK-073 | Tổ hợp xung đột | Kiểm tra panel room conflict bị ẩn khi không có xung đột phòng | Pairwise testing | File chỉ có teacher conflict | 1. Phân tích file chỉ có teacher conflict.<br>2. Quan sát panel room conflict. | Panel xung đột phòng không hiển thị. |  |  |
| HVK-074 | Tổ hợp xung đột | Kiểm tra file có đồng thời xung đột phòng và giảng viên | Pairwise testing | File có cả room conflict và teacher conflict | 1. Chọn file có cả 2 loại xung đột.<br>2. Phân tích file.<br>3. Quan sát kết quả. | Cả `roomConflictCount` và `teacherConflictCount` đều > 0; hai panel đều hiển thị. |  |  |
| HVK-075 | Tổ hợp xung đột | Kiểm tra cả hai panel cùng xuất hiện khi có đủ 2 loại xung đột | Pairwise testing | File có cả room conflict và teacher conflict | 1. Phân tích file có cả hai loại xung đột.<br>2. Quan sát bố cục kết quả. | Panel xung đột phòng và panel xung đột giảng viên đều xuất hiện song song. |  |  |
| HVK-076 | Thông tin file kết quả | Kiểm tra tên file hiển thị đúng trong header kết quả | Equivalence Class Partitioning | File hợp lệ tên xác định | 1. Chọn file có tên xác định.<br>2. Phân tích file.<br>3. Quan sát dòng `File:`. | Tên file hiển thị đúng theo `file.getOriginalFilename()`. |  |  |
| HVK-077 | Badge trạng thái | Kiểm tra badge đỏ hiển thị khi tổng xung đột lớn hơn 0 | Boundary Value Analysis | `totalConflicts > 0` | 1. Phân tích file có xung đột.<br>2. Quan sát badge. | Hiển thị badge đỏ `Có xung đột`. |  |  |
| HVK-078 | Badge trạng thái | Kiểm tra badge xanh hiển thị khi tổng xung đột bằng 0 | Boundary Value Analysis | `totalConflicts = 0` | 1. Phân tích file không xung đột.<br>2. Quan sát badge. | Hiển thị badge xanh `Không có xung đột`. |  |  |
| HVK-079 | Chi tiết room conflict | Kiểm tra mỗi lịch trong room conflict hiển thị mã môn và tên môn | Equivalence Class Partitioning | Có room conflict với nhiều lịch xung đột | 1. Phân tích file có room conflict.<br>2. Mở card chi tiết bất kỳ.<br>3. Quan sát từng dòng chi tiết. | Mỗi dòng chi tiết hiển thị `subjectCode - subjectName`. |  |  |
| HVK-080 | Chi tiết room conflict | Kiểm tra room conflict hiển thị tên giảng viên, mã giảng viên và số SV | Equivalence Class Partitioning | Có room conflict | 1. Phân tích file có room conflict.<br>2. Quan sát phần chi tiết lịch. | Mỗi dòng chi tiết hiển thị `teacherName`, `teacherId`, `studentCount`. |  |  |
| HVK-081 | Chi tiết teacher conflict | Kiểm tra teacher conflict hiển thị phòng và nhóm lớp | Equivalence Class Partitioning | Có teacher conflict | 1. Phân tích file có teacher conflict.<br>2. Quan sát từng dòng chi tiết. | Mỗi dòng chi tiết hiển thị `room` và `classGroup`. |  |  |
| HVK-082 | Chuyển phân tích file khác | Kiểm tra nhấn `Kiểm tra file khác` cho phép phân tích file mới | State Transition | Đã có kết quả từ file A, sau đó chọn file B | 1. Phân tích file A.<br>2. Nhấn `Kiểm tra file khác`.<br>3. Chọn file B và phân tích. | Hệ thống quay lại màn upload và phân tích được file B bình thường. |  |  |
| HVK-083 | Tải file mẫu | Kiểm tra nút tải file mẫu tải đúng file `mau_hau_kiem_tkb.xlsx` | Equivalence Class Partitioning | Đường dẫn file mẫu tồn tại | 1. Nhấn `Tải file mẫu`.<br>2. Quan sát tên file tải xuống. | File mẫu tải xuống có tên `mau_hau_kiem_tkb.xlsx`. |  |  |
| HVK-084 | Xử lý dữ liệu online | Kiểm tra backend bỏ qua các dòng học online hoặc LMS | Equivalence Class Partitioning | File chỉ chứa các dòng có building hoặc room là `online` hoặc `lms` | 1. Chọn file chỉ có dòng online/LMS.<br>2. Phân tích file. | Không sinh `ScheduleEntry` hợp lệ; hệ thống báo không tìm thấy dữ liệu thời khóa biểu. |  |  |
| HVK-085 | Xử lý tuần học | Kiểm tra backend bỏ qua dòng không có tuần học được đánh dấu `x` | Equivalence Class Partitioning | File có dòng hợp lệ cột chính nhưng không có dấu `x` ở tuần nào | 1. Chọn file không có tuần đánh dấu.<br>2. Phân tích file. | Dòng bị bỏ qua; nếu toàn file đều vậy thì hệ thống báo không tìm thấy dữ liệu thời khóa biểu. |  |  |
| HVK-086 | Xử lý dữ liệu thiếu teacherId | Kiểm tra backend bỏ qua dòng thiếu mã giảng viên | Equivalence Class Partitioning | File có dòng thiếu `teacherId` | 1. Chọn file có dòng thiếu mã giảng viên.<br>2. Phân tích file. | Dòng thiếu `teacherId` không được tính là entry hợp lệ. |  |  |
| HVK-087 | Xử lý dữ liệu thiếu phòng | Kiểm tra backend bỏ qua dòng thiếu phòng học hợp lệ | Equivalence Class Partitioning | File có dòng không xác định được `room` đầy đủ | 1. Chọn file có dòng thiếu phòng.<br>2. Phân tích file. | Dòng thiếu phòng không được tính là entry hợp lệ. |  |  |
| HVK-088 | Chuẩn hóa tên phòng | Kiểm tra backend ghép phòng theo định dạng `room - building` trong dữ liệu phân tích | Decision Table | File có `room` và `building` riêng biệt | 1. Chọn file hợp lệ có room và building.<br>2. Phân tích file.<br>3. Quan sát chi tiết conflict. | Tên phòng trong kết quả hiển thị theo định dạng `room - building`. |  |  |
| HVK-089 | Validate format Excel | Kiểm tra file có dưới 44 cột bị coi là sai định dạng | Boundary Value Analysis | File `.xlsx` có 43 cột hoặc ít hơn | 1. Chọn file 43 cột.<br>2. Phân tích file. | Backend trả lỗi file không đúng định dạng thời khóa biểu. |  |  |
| HVK-090 | Bỏ qua header | Kiểm tra backend bỏ qua đúng 3 dòng header đầu tiên | Boundary Value Analysis | File hợp lệ có 3 dòng header và dữ liệu bắt đầu từ dòng 4 | 1. Chọn file chuẩn có dữ liệu từ dòng 4.<br>2. Phân tích file.<br>3. Đối chiếu số entry đọc được. | Dữ liệu chỉ được đọc từ sau 3 dòng header, không lấy nhầm header làm entry. |  |  |
| HVK-091 | Tổng hợp conflict | Kiểm tra tổng số xung đột hiển thị theo số lượng conflict đã được nhóm | Decision Table | File tạo ra nhiều conflict cùng pattern | 1. Chọn file có conflict cùng mẫu lặp lại.<br>2. Phân tích file.<br>3. So sánh tổng số xung đột hiển thị. | Tổng xung đột phản ánh số conflict sau khi nhóm theo pattern, không phải số dòng raw. |  |  |
| HVK-092 | Hiển thị tên file | Kiểm tra file name hiển thị đúng ở result view với file tên dài | Equivalence Class Partitioning | File hợp lệ có tên dài | 1. Chọn file tên dài.<br>2. Phân tích file.<br>3. Quan sát phần `File:`. | Tên file hiển thị đúng, không làm vỡ layout chính. |  |  |
| HVK-093 | Quay lại màn upload | Kiểm tra sau reset kết quả, UI quay về đúng màn upload ban đầu | State Transition | Đã có kết quả phân tích | 1. Phân tích file.<br>2. Nhấn `Kiểm tra file khác`.<br>3. Quan sát UI. | Màn upload ban đầu hiển thị lại đầy đủ card upload và info card. |  |  |
| HVK-094 | Render room conflict list | Kiểm tra danh sách room conflicts render đúng số card nhóm xung đột | Equivalence Class Partitioning | Response có nhiều `roomConflicts` | 1. Phân tích file có nhiều room conflict.<br>2. Đếm số card hiển thị. | Số card room conflict bằng số phần tử `roomConflicts` sau nhóm. |  |  |
| HVK-095 | Render teacher conflict list | Kiểm tra danh sách teacher conflicts render đúng số card nhóm xung đột | Equivalence Class Partitioning | Response có nhiều `teacherConflicts` | 1. Phân tích file có nhiều teacher conflict.<br>2. Đếm số card hiển thị. | Số card teacher conflict bằng số phần tử `teacherConflicts` sau nhóm. |  |  |
| HVK-096 | Nhánh lỗi business | Kiểm tra khi API trả `success=false` thì không vào màn kết quả | Decision Table | API analyze trả `{ success: false, message: ... }` | 1. Chọn file.<br>2. Mock API trả `success=false`.<br>3. Nhấn phân tích. | Hệ thống giữ ở màn upload, hiển thị lỗi và không set `result`. |  |  |
| HVK-097 | Nhánh thành công | Kiểm tra khi API trả `success=true` thì set result và vào màn kết quả | Decision Table | API analyze trả `{ success: true, data: ... }` | 1. Chọn file hợp lệ.<br>2. Mock API trả `success=true`.<br>3. Nhấn phân tích. | Hệ thống set `result`, set `success`, và chuyển sang màn kết quả. |  |  |
| HVK-098 | Hiển thị kích thước file | Kiểm tra kích thước file được format đúng ở card file đã chọn | Equivalence Class Partitioning | Chọn file có kích thước KB hoặc MB | 1. Chọn file hợp lệ.<br>2. Quan sát card file. | Kích thước file hiển thị theo định dạng Bytes, KB hoặc MB phù hợp. |  |  |
| HVK-099 | Tương đương thao tác chọn file | Kiểm tra kéo thả file hợp lệ cho kết quả giống chọn file bằng file picker | Pairwise testing | Cùng một file `.xlsx` hợp lệ, 2 cách nhập liệu | 1. Chọn file bằng file picker.<br>2. Reset.<br>3. Kéo thả chính file đó.<br>4. So sánh trạng thái. | Cả hai cách đều tạo cùng trạng thái `selectedFile`, hiển thị cùng card file đã chọn. |  |  |
| HVK-100 | Trạng thái nút phân tích | Kiểm tra text nút đổi sang `Đang kiểm tra...` trong lúc phân tích | State Transition | Đã chọn file hợp lệ, API phản hồi chậm | 1. Chọn file hợp lệ.<br>2. Nhấn phân tích.<br>3. Quan sát text nút trong lúc loading. | Nút đổi text sang `Đang kiểm tra...` kèm spinner cho đến khi request hoàn tất. |  |  |
