# Test Cases cho Module `/saved-schedules`

Ghi chú:
- Phân loại theo đúng thứ tự: `UI testing` -> `validation test` -> `function test`.
- Cột `Kỹ thuật kiểm thử` chỉ dùng 5 kỹ thuật: `Equivalence Class Partitioning`, `Boundary Value Analysis`, `Decision Table`, `State Transition`, `Pairwise testing`.
- Hai cột `Kết quả thực tế` và `Trạng thái` để trống.

## UI testing

| Mã trường hợp kiểm thử | Chức năng | Mục đích kiểm thử | Kỹ thuật kiểm thử | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| SS-001 | Hiển thị trang | Kiểm tra trang `/saved-schedules` hiển thị đúng tiêu đề và mô tả | Equivalence Class Partitioning | Người dùng đã đăng nhập và truy cập được trang | 1. Đăng nhập hợp lệ.<br>2. Mở `/saved-schedules`.<br>3. Quan sát phần đầu trang. | Hiển thị tiêu đề `Thời Khóa Biểu` và mô tả quản lý các thời khóa biểu đã lưu. |  |  |
| SS-002 | Hiển thị trang | Kiểm tra nút `Xuất Excel` xuất hiện tại header | Equivalence Class Partitioning | Mở trang `/saved-schedules` | 1. Mở trang.<br>2. Quan sát khu vực header. | Nút `Xuất Excel` hiển thị rõ ràng trong header. |  |  |
| SS-003 | Hiển thị trang | Kiểm tra nút `Xóa ngành` và `Xóa Tất Cả` xuất hiện tại thanh thao tác | Equivalence Class Partitioning | Mở trang `/saved-schedules` | 1. Mở trang.<br>2. Quan sát thanh filter và action. | Hiển thị đủ hai nút `Xóa ngành` và `Xóa Tất Cả`. |  |  |
| SS-004 | Hiển thị filter | Kiểm tra hàng filter hiển thị đủ 4 dropdown và nhãn tổng | Equivalence Class Partitioning | Mở trang `/saved-schedules` | 1. Mở trang.<br>2. Quan sát thanh filter. | Có 4 dropdown `năm học`, `học kỳ`, `khóa`, `ngành` và badge `Tổng`. |  |  |
| SS-005 | Trạng thái tải dữ liệu | Kiểm tra bảng hiển thị dòng `Đang tải...` trong lúc gọi API | State Transition | API `/schedules` phản hồi chậm | 1. Mở trang.<br>2. Giữ API chậm.<br>3. Quan sát vùng bảng. | Hiển thị dòng `Đang tải...` cho đến khi API trả dữ liệu. |  |  |
| SS-006 | Trạng thái rỗng | Kiểm tra bảng hiển thị đúng khi không có lịch học đã lưu | Boundary Value Analysis | API `/schedules` trả mảng rỗng | 1. Mock dữ liệu rỗng.<br>2. Mở trang.<br>3. Quan sát bảng. | Bảng hiển thị `Chưa có lịch học nào được lưu`. |  |  |
| SS-007 | Trạng thái nút | Kiểm tra nút `Xuất Excel` bị khóa khi không có dữ liệu hiển thị | Boundary Value Analysis | `groupedSchedules = 0` | 1. Mở trang với dữ liệu rỗng.<br>2. Quan sát nút `Xuất Excel`. | Nút `Xuất Excel` bị disable. |  |  |
| SS-008 | Trạng thái nút | Kiểm tra nút `Xóa ngành` bị khóa khi không có bản ghi nào trong `schedules` | Boundary Value Analysis | `schedules = []` | 1. Mở trang với dữ liệu rỗng.<br>2. Quan sát nút `Xóa ngành`. | Nút `Xóa ngành` bị disable. |  |  |
| SS-009 | Trạng thái nút | Kiểm tra nút `Xóa Tất Cả` bị khóa khi không có bản ghi nào trong `schedules` | Boundary Value Analysis | `schedules = []` | 1. Mở trang với dữ liệu rỗng.<br>2. Quan sát nút `Xóa Tất Cả`. | Nút `Xóa Tất Cả` bị disable. |  |  |
| SS-010 | Hiển thị filter | Kiểm tra giá trị mặc định của dropdown năm học | Equivalence Class Partitioning | Mở trang lần đầu | 1. Mở trang.<br>2. Quan sát dropdown năm học. | Giá trị mặc định là `Tất cả năm học`. |  |  |
| SS-011 | Hiển thị filter | Kiểm tra giá trị mặc định của dropdown học kỳ | Equivalence Class Partitioning | Mở trang lần đầu | 1. Mở trang.<br>2. Quan sát dropdown học kỳ. | Giá trị mặc định là `Tất cả học kỳ`. |  |  |
| SS-012 | Hiển thị filter | Kiểm tra giá trị mặc định của dropdown khóa | Equivalence Class Partitioning | Mở trang lần đầu | 1. Mở trang.<br>2. Quan sát dropdown khóa. | Giá trị mặc định là `Tất cả khóa`. |  |  |
| SS-013 | Hiển thị filter | Kiểm tra giá trị mặc định của dropdown ngành | Equivalence Class Partitioning | Mở trang lần đầu | 1. Mở trang.<br>2. Quan sát dropdown ngành. | Giá trị mặc định là `Tất cả ngành`. |  |  |
| SS-014 | Trạng thái chọn filter | Kiểm tra dropdown năm học đổi style khi có lựa chọn | State Transition | Chọn một năm học hợp lệ | 1. Chọn năm học bất kỳ.<br>2. Quan sát style control. | Dropdown năm học có style active như viền đỏ, nền đỏ nhạt, chữ đậm. |  |  |
| SS-015 | Trạng thái chọn filter | Kiểm tra dropdown học kỳ đổi style khi có lựa chọn | State Transition | Chọn một học kỳ hợp lệ | 1. Chọn học kỳ bất kỳ.<br>2. Quan sát style control. | Dropdown học kỳ có style active như thiết kế. |  |  |
| SS-016 | Trạng thái chọn filter | Kiểm tra dropdown khóa đổi style khi có lựa chọn | State Transition | Chọn một khóa hợp lệ | 1. Chọn khóa bất kỳ.<br>2. Quan sát style control. | Dropdown khóa có style active như thiết kế. |  |  |
| SS-017 | Trạng thái chọn filter | Kiểm tra dropdown ngành đổi style khi có lựa chọn | State Transition | Chọn một ngành hợp lệ | 1. Chọn ngành bất kỳ.<br>2. Quan sát style control. | Dropdown ngành có style active như thiết kế. |  |  |
| SS-018 | Bảng dữ liệu | Kiểm tra header bảng hiển thị đầy đủ các cột thông tin chính | Equivalence Class Partitioning | Có dữ liệu lịch học | 1. Mở trang có dữ liệu.<br>2. Quan sát header bảng. | Bảng có các cột lớp, mã môn, tên môn, năm học, HK, khóa, ngành, hệ ĐT, thứ, kíp, TBĐ, L, sĩ số, phòng, T1-T17, tổng. |  |  |
| SS-019 | Bảng dữ liệu | Kiểm tra phần header của bảng là sticky khi cuộn | State Transition | Có dữ liệu đủ nhiều để bảng cuộn | 1. Mở trang có nhiều dữ liệu.<br>2. Cuộn xuống trong bảng.<br>3. Quan sát header. | Header bảng vẫn giữ cố định ở phía trên vùng cuộn. |  |  |
| SS-020 | Bảng dữ liệu | Kiểm tra vùng bảng có thể cuộn khi danh sách dài | Equivalence Class Partitioning | Có nhiều dòng dữ liệu | 1. Mở trang có nhiều dữ liệu.<br>2. Cuộn dọc trong vùng bảng. | Vùng bảng cuộn được, không làm vỡ layout trang. |  |  |
| SS-021 | Bảng dữ liệu | Kiểm tra màu nền hàng được tô xen kẽ theo nhóm `mã môn + lớp` | State Transition | Có ít nhất 2 nhóm lớp khác nhau | 1. Mở trang có nhiều nhóm lớp.<br>2. Quan sát màu hàng liên tiếp. | Các hàng cùng nhóm có cùng màu; khi chuyển nhóm thì màu đổi xen kẽ. |  |  |
| SS-022 | Hiển thị ô dữ liệu | Kiểm tra ô phòng hiển thị theo định dạng `name-building` khi có phòng | Equivalence Class Partitioning | Bản ghi có thông tin phòng | 1. Mở trang có bản ghi có phòng.<br>2. Quan sát cột phòng. | Giá trị hiển thị đúng dạng `101-A2` hoặc tương đương. |  |  |
| SS-023 | Hiển thị ô dữ liệu | Kiểm tra ô phòng để trống khi bản ghi không có phòng | Boundary Value Analysis | Bản ghi có `room = null` | 1. Mở trang có bản ghi không có phòng.<br>2. Quan sát cột phòng. | Ô phòng để trống, không hiển thị lỗi hoặc `undefined`. |  |  |
| SS-024 | Hiển thị ô dữ liệu | Kiểm tra các trường template thiếu dữ liệu hiển thị dấu `-` | Boundary Value Analysis | Bản ghi thiếu `tkbTemplate.dayOfWeek/kip/startPeriod/periodLength` | 1. Mở trang với bản ghi thiếu template.<br>2. Quan sát các cột tương ứng. | Các cột thiếu dữ liệu hiển thị `-`. |  |  |
| SS-025 | Hiển thị ô dữ liệu | Kiểm tra cột tổng hiển thị `0` khi `totalUsed` không có giá trị | Boundary Value Analysis | Bản ghi có `tkbTemplate.totalUsed = null/undefined` | 1. Mở trang với dữ liệu phù hợp.<br>2. Quan sát cột tổng. | Cột tổng hiển thị `0`. |  |  |
| SS-026 | Modal xóa tất cả | Kiểm tra nhấn `Xóa Tất Cả` mở modal xác nhận | State Transition | Có dữ liệu lịch học | 1. Mở trang có dữ liệu.<br>2. Nhấn `Xóa Tất Cả`. | Modal xóa tất cả mở ra với phần header cảnh báo màu đỏ. |  |  |
| SS-027 | Modal xóa theo ngành | Kiểm tra nhấn `Xóa ngành` mở modal xác nhận | State Transition | Có dữ liệu lịch học | 1. Mở trang có dữ liệu.<br>2. Nhấn `Xóa ngành`. | Modal xóa theo ngành mở ra với phần header cảnh báo màu đỏ. |  |  |
| SS-028 | Modal xóa tất cả | Kiểm tra modal xóa tất cả hiển thị đủ 2 dropdown năm học và học kỳ | Equivalence Class Partitioning | Modal xóa tất cả đang mở | 1. Mở modal xóa tất cả.<br>2. Quan sát nội dung modal. | Modal hiển thị dropdown năm học và dropdown học kỳ. |  |  |
| SS-029 | Modal xóa theo ngành | Kiểm tra modal xóa theo ngành hiển thị đủ 3 dropdown năm học, học kỳ, ngành | Equivalence Class Partitioning | Modal xóa theo ngành đang mở | 1. Mở modal xóa theo ngành.<br>2. Quan sát nội dung modal. | Modal hiển thị đủ năm học, học kỳ và ngành cần xóa. |  |  |
| SS-030 | Modal thao tác | Kiểm tra cả hai modal đều có nút `Hủy` và `Xóa` | Decision Table | Mở lần lượt 2 modal | 1. Mở modal xóa tất cả.<br>2. Mở modal xóa theo ngành.<br>3. Quan sát footer modal. | Cả hai modal đều có nút `Hủy` và `Xóa`. |  |  |
| SS-031 | Trạng thái loading modal | Kiểm tra modal xóa tất cả hiển thị spinner khi đang xử lý | State Transition | API xóa tất cả phản hồi chậm | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu hợp lệ.<br>3. Nhấn `Xóa` và giữ API chậm. | Nút `Xóa` chuyển sang `Đang xóa...` kèm spinner. |  |  |
| SS-032 | Trạng thái loading modal | Kiểm tra modal xóa theo ngành hiển thị spinner khi đang xử lý | State Transition | API xóa theo ngành phản hồi chậm | 1. Mở modal xóa theo ngành.<br>2. Chọn dữ liệu hợp lệ.<br>3. Nhấn `Xóa` và giữ API chậm. | Nút `Xóa` chuyển sang `Đang xóa...` kèm spinner. |  |  |

## validation test

| Mã trường hợp kiểm thử | Chức năng | Mục đích kiểm thử | Kỹ thuật kiểm thử | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| SS-033 | Validation modal xóa tất cả | Kiểm tra không cho xóa khi chưa chọn năm học | Decision Table | Modal xóa tất cả mở, `deleteAllAcademicYear = ''` | 1. Mở modal xóa tất cả.<br>2. Để trống năm học.<br>3. Nhấn `Xóa`. | Hiển thị toast yêu cầu chọn năm học và học kỳ, không thực hiện xóa. |  |  |
| SS-034 | Validation modal xóa tất cả | Kiểm tra không cho xóa khi chưa chọn học kỳ | Decision Table | Modal xóa tất cả mở, đã có năm học nhưng `deleteAllSemester = ''` | 1. Mở modal xóa tất cả.<br>2. Chọn năm học.<br>3. Không chọn học kỳ.<br>4. Nhấn `Xóa`. | Hiển thị toast yêu cầu chọn năm học và học kỳ, không thực hiện xóa. |  |  |
| SS-035 | Validation modal xóa tất cả | Kiểm tra không cho xóa khi cả năm học và học kỳ đều bỏ trống | Decision Table | `deleteAllAcademicYear = ''`, `deleteAllSemester = ''` | 1. Mở modal xóa tất cả.<br>2. Không chọn giá trị nào.<br>3. Nhấn `Xóa`. | Hiển thị thông báo lỗi, không gửi request xóa. |  |  |
| SS-036 | Validation modal xóa tất cả | Kiểm tra học kỳ bị disable khi chưa chọn năm học trong modal xóa tất cả | State Transition | Modal xóa tất cả đang mở, năm học rỗng | 1. Mở modal xóa tất cả.<br>2. Quan sát dropdown học kỳ. | Dropdown học kỳ bị disable khi chưa chọn năm học. |  |  |
| SS-037 | Validation modal xóa tất cả | Kiểm tra đổi năm học sẽ reset học kỳ trong modal xóa tất cả | State Transition | Modal xóa tất cả đã chọn năm học và học kỳ | 1. Mở modal xóa tất cả.<br>2. Chọn năm học và học kỳ.<br>3. Đổi sang năm học khác. | Giá trị học kỳ bị reset về rỗng. |  |  |
| SS-038 | Validation modal xóa tất cả | Kiểm tra trường hợp chọn năm học và học kỳ nhưng không có dữ liệu để xóa | Equivalence Class Partitioning | Chọn tổ hợp năm học-học kỳ không có bản ghi nào khớp | 1. Mở modal xóa tất cả.<br>2. Chọn tổ hợp không có dữ liệu.<br>3. Nhấn `Xóa`. | Hiển thị toast `Không tìm thấy lịch học nào`, modal đóng và dữ liệu chọn được reset. |  |  |
| SS-039 | Validation modal xóa tất cả | Kiểm tra trường hợp không tìm thấy `semesterId` trong danh sách học kỳ | Equivalence Class Partitioning | `deleteAllAcademicYear`, `deleteAllSemester` có giá trị nhưng không map được trong mảng `semesters` | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu bất thường.<br>3. Nhấn `Xóa`. | Hiển thị toast `Không tìm thấy thông tin học kỳ`, loading dừng, modal vẫn mở. |  |  |
| SS-040 | Validation modal xóa theo ngành | Kiểm tra không cho xóa khi chưa chọn ngành | Decision Table | Modal xóa theo ngành mở, `majorToDelete = ''` | 1. Mở modal xóa theo ngành.<br>2. Chưa chọn ngành.<br>3. Nhấn `Xóa`. | Hiển thị toast yêu cầu chọn ngành, không thực hiện xóa. |  |  |
| SS-041 | Validation modal xóa theo ngành | Kiểm tra không cho xóa khi chưa chọn năm học | Decision Table | `deleteMajorAcademicYear = ''` | 1. Mở modal xóa theo ngành.<br>2. Chưa chọn năm học.<br>3. Nhấn `Xóa`. | Hiển thị toast yêu cầu chọn năm học, không thực hiện xóa. |  |  |
| SS-042 | Validation modal xóa theo ngành | Kiểm tra không cho xóa khi chưa chọn học kỳ | Decision Table | Đã có năm học, `deleteMajorSemester = ''` | 1. Mở modal xóa theo ngành.<br>2. Chọn năm học.<br>3. Không chọn học kỳ.<br>4. Nhấn `Xóa`. | Hiển thị toast yêu cầu chọn học kỳ, không thực hiện xóa. |  |  |
| SS-043 | Validation modal xóa theo ngành | Kiểm tra học kỳ bị disable khi chưa chọn năm học trong modal xóa theo ngành | State Transition | Modal xóa theo ngành đang mở, năm học rỗng | 1. Mở modal xóa theo ngành.<br>2. Quan sát dropdown học kỳ. | Dropdown học kỳ bị disable khi chưa chọn năm học. |  |  |
| SS-044 | Validation modal xóa theo ngành | Kiểm tra đổi năm học sẽ reset học kỳ trong modal xóa theo ngành | State Transition | Modal xóa theo ngành đã chọn năm học và học kỳ | 1. Mở modal xóa theo ngành.<br>2. Chọn năm học và học kỳ.<br>3. Đổi năm học. | Giá trị học kỳ bị reset về rỗng. |  |  |
| SS-045 | Validation modal xóa theo ngành | Kiểm tra trường hợp không có dữ liệu khớp ngành-năm học-học kỳ cần xóa | Equivalence Class Partitioning | Chọn ngành, năm học, học kỳ nhưng không có bản ghi khớp | 1. Mở modal xóa theo ngành.<br>2. Chọn tổ hợp không có dữ liệu.<br>3. Nhấn `Xóa`. | Hiển thị toast không tìm thấy lịch học, modal đóng và 3 trường được reset. |  |  |
| SS-046 | Validation export | Kiểm tra nút `Xuất Excel` bị khóa khi sau lọc không còn bản ghi nào | Boundary Value Analysis | `schedules > 0` nhưng `groupedSchedules = 0` sau khi lọc | 1. Áp dụng filter để không còn dữ liệu hiển thị.<br>2. Quan sát nút `Xuất Excel`. | Nút `Xuất Excel` bị disable. |  |  |
| SS-047 | Validation xóa | Kiểm tra các nút xóa vẫn enable khi tổng `schedules` còn dữ liệu dù kết quả lọc hiện tại rỗng | Decision Table | `schedules > 0`, `filteredSchedules = 0` | 1. Áp dụng filter để bảng không còn dữ liệu.<br>2. Quan sát nút `Xóa ngành` và `Xóa Tất Cả`. | Hai nút vẫn enable vì điều kiện code dựa trên `schedules.length`. |  |  |
| SS-048 | Validation fallback filter | Kiểm tra danh sách ngành trong filter chỉ lấy từ `schedules` hiện có | Equivalence Class Partitioning | `schedules` có một số ngành, API majors không được dùng ở màn này | 1. Mở trang có dữ liệu.<br>2. Mở dropdown ngành.<br>3. Đối chiếu với dữ liệu bảng. | Dropdown ngành chỉ chứa các ngành thực sự có trong `schedules`. |  |  |
| SS-049 | Validation fallback filter | Kiểm tra danh sách khóa fallback từ `schedules` khi API class years không có dữ liệu | Decision Table | `classYears = []`, `schedules` có dữ liệu khóa | 1. Mock API class years rỗng.<br>2. Mở trang.<br>3. Mở dropdown khóa. | Dropdown khóa vẫn có dữ liệu lấy từ `schedules`. |  |  |
| SS-050 | Validation fallback filter | Kiểm tra danh sách năm học fallback từ `schedules` khi API semesters không có dữ liệu | Decision Table | `academicYears = []`, `schedules` có dữ liệu năm học | 1. Mock API semesters lỗi hoặc rỗng.<br>2. Mở trang.<br>3. Mở dropdown năm học. | Dropdown năm học vẫn có dữ liệu lấy từ `schedules`. |  |  |
| SS-051 | Validation fallback filter | Kiểm tra danh sách học kỳ fallback từ `schedules` khi API semesters không có dữ liệu | Decision Table | `semesters = []`, `schedules` có dữ liệu học kỳ | 1. Mock API semesters lỗi hoặc rỗng.<br>2. Mở trang.<br>3. Mở dropdown học kỳ. | Dropdown học kỳ vẫn hiển thị dữ liệu fallback từ `schedules`. |  |  |
| SS-052 | Validation dữ liệu tuần học | Kiểm tra `getWeekValue` không làm vỡ trang khi `weekSchedule` không phải JSON hợp lệ | Equivalence Class Partitioning | `weekSchedule` là chuỗi JSON lỗi | 1. Mở trang với bản ghi có `weekSchedule` lỗi.<br>2. Quan sát bảng và console nếu cần. | Trang không crash; các ô tuần của bản ghi đó hiển thị rỗng. |  |  |
| SS-053 | Validation dữ liệu tuần học | Kiểm tra `getWeekValue` trả rỗng khi `weekSchedule` không tồn tại | Boundary Value Analysis | `tkbTemplate.weekSchedule = null/undefined` | 1. Mở trang với bản ghi thiếu `weekSchedule`.<br>2. Quan sát cột tuần. | Các ô tuần để trống, không phát sinh lỗi hiển thị. |  |  |
| SS-054 | Validation tải filter | Kiểm tra lỗi tải dữ liệu filter không làm hỏng giao diện trang | Equivalence Class Partitioning | API class years hoặc semesters lỗi | 1. Chặn API filter data.<br>2. Mở trang.<br>3. Quan sát giao diện tổng thể. | Trang vẫn hiển thị bình thường, chỉ thiếu dữ liệu filter tương ứng. |  |  |
| SS-055 | Validation lỗi xóa tất cả | Kiểm tra lỗi trong luồng xóa tất cả hiển thị đúng toast lỗi | Equivalence Class Partitioning | Một API trong luồng xóa tất cả trả lỗi | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu hợp lệ.<br>3. Mock lỗi backend.<br>4. Nhấn `Xóa`. | Hiển thị toast `Lỗi khi xóa lịch học...`, loading dừng lại. |  |  |
| SS-056 | Validation lỗi xóa theo ngành | Kiểm tra lỗi trong luồng xóa theo ngành hiển thị toast lỗi | Equivalence Class Partitioning | Một API trong luồng xóa theo ngành trả lỗi | 1. Mở modal xóa theo ngành.<br>2. Chọn dữ liệu hợp lệ.<br>3. Mock lỗi backend.<br>4. Nhấn `Xóa`. | Hiển thị toast `Lỗi khi xóa lịch học`, loading dừng lại. |  |  |
| SS-057 | Validation đóng modal | Kiểm tra nhấn `Hủy` ở modal xóa tất cả reset toàn bộ lựa chọn tạm | State Transition | Modal xóa tất cả đang mở, đã chọn dữ liệu | 1. Mở modal xóa tất cả.<br>2. Chọn năm học và học kỳ.<br>3. Nhấn `Hủy`.<br>4. Mở lại modal. | Các trường trong modal được reset hoàn toàn. |  |  |
| SS-058 | Validation đóng modal | Kiểm tra nhấn `Hủy` ở modal xóa theo ngành reset toàn bộ lựa chọn tạm | State Transition | Modal xóa theo ngành đang mở, đã chọn dữ liệu | 1. Mở modal xóa theo ngành.<br>2. Chọn năm học, học kỳ, ngành.<br>3. Nhấn `Hủy`.<br>4. Mở lại modal. | Các trường trong modal được reset hoàn toàn. |  |  |
| SS-059 | Validation đóng modal | Kiểm tra click nền tối đóng modal xóa tất cả và reset dữ liệu | State Transition | Modal xóa tất cả đang mở | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu.<br>3. Click ra ngoài modal.<br>4. Mở lại modal. | Modal đóng và dữ liệu tạm được reset. |  |  |
| SS-060 | Validation đóng modal | Kiểm tra click nền tối đóng modal xóa theo ngành và reset dữ liệu | State Transition | Modal xóa theo ngành đang mở | 1. Mở modal xóa theo ngành.<br>2. Chọn dữ liệu.<br>3. Click ra ngoài modal.<br>4. Mở lại modal. | Modal đóng và dữ liệu tạm được reset. |  |  |

## function test

| Mã trường hợp kiểm thử | Chức năng | Mục đích kiểm thử | Kỹ thuật kiểm thử | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái |
|---|---|---|---|---|---|---|---|---|
| SS-061 | Tải danh sách | Kiểm tra API `/schedules` thành công sẽ hiển thị các bản ghi trong bảng | Equivalence Class Partitioning | API `/schedules` trả danh sách hợp lệ | 1. Mở trang.<br>2. Chờ API hoàn tất.<br>3. Quan sát bảng. | Tất cả bản ghi trả về được render thành các dòng dữ liệu trên bảng. |  |  |
| SS-062 | Tải dữ liệu filter | Kiểm tra API `class years` và `semesters` thành công sẽ nạp dữ liệu cho bộ lọc | Equivalence Class Partitioning | API filter data trả dữ liệu hợp lệ | 1. Mở trang.<br>2. Chờ tải filter data.<br>3. Mở các dropdown. | Bộ lọc năm học, học kỳ, khóa hiển thị dữ liệu lấy từ API. |  |  |
| SS-063 | Nhóm dữ liệu theo lớp | Kiểm tra logic nhóm theo `subjectCode-major-classNumber` hoạt động đúng | Decision Table | Có nhiều schedule cùng mã môn, ngành, lớp nhưng khác buổi học | 1. Mở trang với dữ liệu phù hợp.<br>2. Quan sát các dòng thuộc cùng một lớp.<br>3. Đối chiếu dữ liệu nhóm. | Các buổi của cùng lớp được coi là cùng một nhóm logic để tô màu và xuất dữ liệu. |  |  |
| SS-064 | Sắp xếp dữ liệu trước khi nhóm | Kiểm tra logic sắp xếp theo `id` tăng dần trước khi nhóm | Boundary Value Analysis | Có dữ liệu cùng nhóm nhưng `id` không theo thứ tự ban đầu | 1. Mở trang với dữ liệu có `id` xen kẽ.<br>2. Quan sát thứ tự hiển thị. | Dữ liệu được xử lý theo thứ tự `id` tăng dần trước khi nhóm màu. |  |  |
| SS-065 | Lọc theo năm học | Kiểm tra bộ lọc năm học dùng giá trị `subject.semester.academicYear` | Equivalence Class Partitioning | Chọn 1 năm học có dữ liệu | 1. Chọn năm học.<br>2. Quan sát danh sách kết quả. | Chỉ các bản ghi có `subject.semester.academicYear` trùng filter được giữ lại. |  |  |
| SS-066 | Lọc theo học kỳ | Kiểm tra bộ lọc học kỳ dùng giá trị `subject.semester.semesterName` | Equivalence Class Partitioning | Chọn 1 học kỳ có dữ liệu | 1. Chọn học kỳ.<br>2. Quan sát danh sách kết quả. | Chỉ các bản ghi có `subject.semester.semesterName` trùng filter được giữ lại. |  |  |
| SS-067 | Lọc theo khóa | Kiểm tra bộ lọc khóa dùng giá trị `studentYear` | Equivalence Class Partitioning | Chọn 1 khóa có dữ liệu | 1. Chọn khóa.<br>2. Quan sát danh sách kết quả. | Chỉ các bản ghi có `studentYear` trùng filter được giữ lại. |  |  |
| SS-068 | Lọc theo ngành | Kiểm tra bộ lọc ngành dùng giá trị `subject.major.majorCode` | Equivalence Class Partitioning | Chọn 1 ngành có dữ liệu | 1. Chọn ngành.<br>2. Quan sát danh sách kết quả. | Chỉ các bản ghi có `subject.major.majorCode` trùng filter được giữ lại. |  |  |
| SS-069 | Lọc kết hợp | Kiểm tra kết hợp `năm học + học kỳ` | Pairwise testing | Chọn 1 năm học và 1 học kỳ thuộc năm đó | 1. Chọn năm học.<br>2. Chọn học kỳ.<br>3. Quan sát bảng. | Chỉ dữ liệu khớp cả hai điều kiện được hiển thị. |  |  |
| SS-070 | Lọc kết hợp | Kiểm tra kết hợp `năm học + khóa` | Pairwise testing | Chọn 1 năm học và 1 khóa | 1. Chọn năm học.<br>2. Chọn khóa.<br>3. Quan sát bảng. | Chỉ dữ liệu khớp năm học và khóa được hiển thị. |  |  |
| SS-071 | Lọc kết hợp | Kiểm tra kết hợp `năm học + ngành` | Pairwise testing | Chọn 1 năm học và 1 ngành | 1. Chọn năm học.<br>2. Chọn ngành.<br>3. Quan sát bảng. | Chỉ dữ liệu khớp năm học và ngành được hiển thị. |  |  |
| SS-072 | Lọc kết hợp | Kiểm tra kết hợp `học kỳ + khóa` | Pairwise testing | Chọn 1 học kỳ và 1 khóa | 1. Chọn học kỳ.<br>2. Chọn khóa.<br>3. Quan sát bảng. | Chỉ dữ liệu khớp học kỳ và khóa được hiển thị. |  |  |
| SS-073 | Lọc kết hợp | Kiểm tra kết hợp `học kỳ + ngành` | Pairwise testing | Chọn 1 học kỳ và 1 ngành | 1. Chọn học kỳ.<br>2. Chọn ngành.<br>3. Quan sát bảng. | Chỉ dữ liệu khớp học kỳ và ngành được hiển thị. |  |  |
| SS-074 | Lọc kết hợp | Kiểm tra kết hợp `khóa + ngành` | Pairwise testing | Chọn 1 khóa và 1 ngành | 1. Chọn khóa.<br>2. Chọn ngành.<br>3. Quan sát bảng. | Chỉ dữ liệu khớp khóa và ngành được hiển thị. |  |  |
| SS-075 | Lọc kết hợp | Kiểm tra kết hợp cả 4 filter cùng lúc | Decision Table | Chọn năm học, học kỳ, khóa, ngành cùng lúc | 1. Chọn đủ 4 filter.<br>2. Quan sát bảng. | Chỉ bản ghi thỏa đồng thời 4 điều kiện mới hiển thị. |  |  |
| SS-076 | Reset filter phụ thuộc | Kiểm tra đổi năm học sẽ xóa giá trị học kỳ đang chọn | State Transition | Đang chọn năm học A và học kỳ A1 | 1. Chọn năm học A và học kỳ A1.<br>2. Đổi sang năm học B.<br>3. Quan sát học kỳ. | Học kỳ được reset về mặc định để tránh lệch dữ liệu. |  |  |
| SS-077 | Bộ lọc học kỳ theo năm học | Kiểm tra danh sách học kỳ trong filter chính được lọc theo năm học đã chọn | Decision Table | Có nhiều học kỳ thuộc nhiều năm học | 1. Chọn năm học.<br>2. Mở dropdown học kỳ. | Chỉ hiển thị các học kỳ thuộc năm học đã chọn. |  |  |
| SS-078 | Tổng số | Kiểm tra badge `Tổng` hiển thị đúng `filteredSchedules.length` | Decision Table | Áp dụng các tổ hợp filter khác nhau | 1. Thay đổi filter nhiều lần.<br>2. Đối chiếu số bản ghi thực tế. | Badge `Tổng` cập nhật chính xác theo tập dữ liệu đã lọc. |  |  |
| SS-079 | Xuất Excel | Kiểm tra file được xuất với tên đúng định dạng | Equivalence Class Partitioning | Có dữ liệu để xuất | 1. Nhấn `Xuất Excel`.<br>2. Quan sát tên file tải xuống. | Tên file có dạng `tkb_da_luu_YYYY-MM-DD.xlsx`. |  |  |
| SS-080 | Xuất Excel | Kiểm tra workbook chứa sheet tên `TKB` | Equivalence Class Partitioning | Có dữ liệu để xuất | 1. Xuất file Excel.<br>2. Mở file. | Workbook có sheet tên `TKB`. |  |  |
| SS-081 | Xuất Excel | Kiểm tra số dòng dữ liệu trong Excel bằng số schedule đã lọc, không phải số nhóm | Decision Table | Có nhiều schedule trong cùng nhóm lớp | 1. Áp dụng filter.<br>2. Xuất Excel.<br>3. So sánh số dòng trong file với `filteredSchedules`. | Mỗi schedule được xuất thành một dòng; tổng số dòng dữ liệu bằng `filteredSchedules.length`. |  |  |
| SS-082 | Xuất Excel | Kiểm tra file Excel chứa đủ các cột đầu ra như code | Decision Table | Có dữ liệu để xuất | 1. Xuất Excel.<br>2. Mở file.<br>3. Kiểm tra hàng tiêu đề. | File có đủ các cột lớp, mã môn, tên môn, năm học, học kỳ, khóa, ngành, hệ đặc thù, thứ, kíp, TBĐ, L, sĩ số, phòng, T1-T18, tổng. |  |  |
| SS-083 | Xuất Excel | Kiểm tra cột phòng trong Excel dùng cùng định dạng `name-building` | Equivalence Class Partitioning | Có bản ghi có phòng | 1. Xuất Excel.<br>2. Mở file.<br>3. Kiểm tra cột phòng. | Giá trị phòng trong Excel có định dạng `name-building`. |  |  |
| SS-084 | Xuất Excel | Kiểm tra cột `Tổng` trong Excel được tính bằng số tuần có `x` nhân `periodLength` | Decision Table | Bản ghi có `weekSchedule` hợp lệ và `periodLength` xác định | 1. Xuất Excel.<br>2. Mở file.<br>3. Đối chiếu số ô `x` và `periodLength`. | Giá trị tổng trong file Excel đúng theo công thức code. |  |  |
| SS-085 | Xuất Excel | Kiểm tra xuất file khi sau lọc chỉ còn đúng 1 bản ghi | Boundary Value Analysis | Sau lọc còn 1 bản ghi | 1. Chọn filter để còn 1 bản ghi.<br>2. Xuất Excel.<br>3. Mở file. | File được tạo bình thường với 1 dòng dữ liệu, không thiếu tiêu đề. |  |  |
| SS-086 | Hàm lấy tuần học | Kiểm tra `getWeekValue` trả `x` khi phần tử tuần bằng 1 | Boundary Value Analysis | `weekSchedule` có giá trị 1 tại tuần cần kiểm tra | 1. Mở trang với dữ liệu tuần phù hợp.<br>2. Quan sát ô tuần tương ứng. | Ô tuần tương ứng hiển thị `x`. |  |  |
| SS-087 | Hàm lấy tuần học | Kiểm tra `getWeekValue` trả rỗng khi phần tử tuần bằng 0 | Boundary Value Analysis | `weekSchedule` có giá trị 0 tại tuần cần kiểm tra | 1. Mở trang với dữ liệu tuần phù hợp.<br>2. Quan sát ô tuần tương ứng. | Ô tuần tương ứng để trống. |  |  |
| SS-088 | Xóa tất cả theo kỳ | Kiểm tra luồng xóa thành công khi có phòng được sử dụng | Decision Table | Kỳ có lịch học, có `room.id`, `semesterId` hợp lệ, user có `id` | 1. Mở modal xóa tất cả.<br>2. Chọn kỳ có dữ liệu và phòng đã dùng.<br>3. Nhấn `Xóa`. | Room occupancies của kỳ bị xóa, schedules của kỳ bị xóa, lastSlotIdx Redis được reset, trạng thái phòng chuyển `AVAILABLE`, hiển thị toast thành công. |  |  |
| SS-089 | Xóa tất cả theo kỳ | Kiểm tra luồng xóa thành công khi không có phòng nào cần giải phóng | Decision Table | Kỳ có lịch học nhưng không có `room.id` nào | 1. Mở modal xóa tất cả.<br>2. Chọn kỳ phù hợp.<br>3. Nhấn `Xóa`. | Schedules bị xóa thành công, không lỗi khi danh sách phòng rỗng, toast thành công hiển thị. |  |  |
| SS-090 | Xóa tất cả theo kỳ | Kiểm tra luồng xóa vẫn hoàn tất khi localStorage không có `user.id` | Decision Table | Kỳ có dữ liệu, localStorage user không có `id` | 1. Xóa hoặc sửa `user.id` trong localStorage.<br>2. Mở modal xóa tất cả.<br>3. Chọn dữ liệu hợp lệ và nhấn `Xóa`. | Hệ thống bỏ qua bước reset Redis, vẫn xóa schedules và giải phóng phòng thành công. |  |  |
| SS-091 | Xóa tất cả theo kỳ | Kiểm tra sau khi xóa thành công modal đóng và trường lựa chọn được reset | State Transition | Xóa tất cả thành công | 1. Thực hiện xóa tất cả thành công.<br>2. Quan sát modal và trạng thái các field. | Modal đóng, `deleteAllAcademicYear` và `deleteAllSemester` được reset về rỗng. |  |  |
| SS-092 | Xóa tất cả theo kỳ | Kiểm tra danh sách được tải lại sau khi xóa tất cả thành công | State Transition | Xóa tất cả thành công | 1. Xóa tất cả thành công.<br>2. Quan sát bảng sau thao tác. | Hệ thống gọi lại tải danh sách; dữ liệu vừa xóa không còn xuất hiện trên bảng. |  |  |
| SS-093 | Xóa tất cả theo kỳ | Kiểm tra lỗi ở bước xóa room occupancies làm dừng luồng xóa | Decision Table | API `DELETE /v1/room-occupancies/semester/{id}` lỗi | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu hợp lệ.<br>3. Mock lỗi ở API xóa occupancies.<br>4. Nhấn `Xóa`. | Hiển thị toast lỗi; không coi thao tác là thành công. |  |  |
| SS-094 | Xóa tất cả theo kỳ | Kiểm tra lỗi ở bước xóa schedules làm dừng luồng xóa | Decision Table | API `DELETE /schedules/{id}` lỗi | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu hợp lệ.<br>3. Mock lỗi xóa schedules.<br>4. Nhấn `Xóa`. | Hiển thị toast lỗi; loading dừng; modal không đóng tự động. |  |  |
| SS-095 | Xóa tất cả theo kỳ | Kiểm tra lỗi ở bước cập nhật trạng thái phòng làm toàn bộ luồng báo lỗi | Decision Table | API `PATCH /rooms/bulk-status` lỗi | 1. Mở modal xóa tất cả.<br>2. Chọn dữ liệu có phòng.<br>3. Mock lỗi cập nhật phòng.<br>4. Nhấn `Xóa`. | Hệ thống hiển thị toast lỗi cho thao tác xóa, không báo thành công. |  |  |
| SS-096 | Xóa theo ngành | Kiểm tra xóa theo ngành thành công khi có phòng được sử dụng | Decision Table | Có dữ liệu khớp ngành-kỳ, có `room.id`, có `semesterId` | 1. Mở modal xóa ngành.<br>2. Chọn ngành, năm học, học kỳ hợp lệ.<br>3. Nhấn `Xóa`. | Schedules đúng phạm vi bị xóa, phòng liên quan được chuyển `AVAILABLE`, room occupancies của kỳ bị xóa nếu tìm thấy `semesterId`, toast thành công hiển thị. |  |  |
| SS-097 | Xóa theo ngành | Kiểm tra xóa theo ngành thành công khi không có phòng nào cần giải phóng | Decision Table | Có dữ liệu khớp nhưng không có `room.id` | 1. Mở modal xóa ngành.<br>2. Chọn dữ liệu phù hợp.<br>3. Nhấn `Xóa`. | Schedules bị xóa thành công, không lỗi khi danh sách phòng rỗng. |  |  |
| SS-098 | Xóa theo ngành | Kiểm tra xóa theo ngành vẫn thành công khi không tìm thấy `semesterId` trong mảng học kỳ | Decision Table | `currentSemester` không tìm thấy nhưng schedulesToDelete có dữ liệu | 1. Mở modal xóa ngành.<br>2. Chọn dữ liệu hợp lệ nhưng không map được semester object.<br>3. Nhấn `Xóa`. | Hệ thống vẫn xóa schedules và cập nhật phòng; bỏ qua bước xóa room occupancies. |  |  |
| SS-099 | Xóa theo ngành | Kiểm tra sau khi xóa theo ngành thành công modal đóng, field reset và danh sách reload | State Transition | Xóa theo ngành thành công | 1. Thực hiện xóa theo ngành thành công.<br>2. Quan sát modal và bảng. | Modal đóng, các field reset về rỗng, bảng được tải lại và không còn dữ liệu vừa xóa. |  |  |
| SS-100 | Xóa theo ngành | Kiểm tra lỗi ở một bước trong luồng xóa theo ngành sẽ hiển thị lỗi và dừng thao tác | Decision Table | Mock lỗi tại xóa schedules hoặc cập nhật phòng hoặc xóa occupancies | 1. Mở modal xóa ngành.<br>2. Chọn dữ liệu hợp lệ.<br>3. Mock lỗi ở một bước bất kỳ.<br>4. Nhấn `Xóa`. | Hiển thị toast lỗi, loading dừng, không báo thành công. |  |  |
